<?php

use Illuminate\Foundation\Auth\User;

return [

    /*
    |--------------------------------------------------------------------------
    | Authentication Defaults
    |--------------------------------------------------------------------------
    |
    | Пользовательской аутентификации у сервиса нет (s2s Bearer-токены,
    | см. блок api ниже) — guards оставлены в минимальной комплектации,
    | которую ожидает фреймворк.
    |
    */

    'defaults' => [
        'guard' => 'web',
        'passwords' => 'users',
    ],

    'guards' => [
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],
    ],

    'providers' => [
        'users' => [
            'driver' => 'eloquent',
            'model' => User::class,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | API-токены (service-to-service)
    |--------------------------------------------------------------------------
    |
    | Список Bearer-токенов через запятую — по одному на сервис-клиент,
    | чтобы отзыв токена одного клиента не задевал остальных.
    |
    */

    'api' => [
        'tokens' => env('API_AUTH_TOKENS', ''),
    ],

];
