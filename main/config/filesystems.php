<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Файловые диски
    |--------------------------------------------------------------------------
    |
    | Используется только локальный диск (файлы отчётов). Продакшн-вектор —
    | объектное хранилище (S3) добавлением диска без изменения кода.
    |
    */

    'default' => env('FILESYSTEM_DISK', 'local'),

    'disks' => [

        'local' => [
            'driver' => 'local',
            'root' => storage_path('app/private'),
            'serve' => true,
            'throw' => false,
            'report' => false,
        ],

    ],

];
